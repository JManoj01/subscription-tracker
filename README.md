# Subscription Tracker

Track subscriptions and free trials. Add, edit, delete; see monthly/yearly totals and trial expiry alerts.

**Run:** `mvn compile exec:java -Dexec.mainClass="com.subscriptiontracker.Main"` → http://localhost:8080

**Tech stack:** Java 17, Javalin, H2, Gson. Frontend: HTML, CSS, vanilla JS.
